package home

import (
    "github.com/TylerGilman/nereus_main_site/views/layouts"
)

templ Index() {
    @layouts.Base("home") {
        @Partial()
    }
}

templ Partial() {
    <div id="content" hx-swap-oob="true" class=" container mx-auto px-4 py-8 items-start justify-center h-full">
        <div class="mb-8 bg-white shadow-lg border-t-2 rounded-lg p-6">
           Tyler@Nereus 
        </div>
    </div>
}